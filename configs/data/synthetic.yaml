# synthetic.yaml — Synthetic data generation settings.
#
# Status: not yet implemented.
# Pipeline: (planned) scripts/data/generate_synthetic.py — currently a stub that
#           raises NotImplementedError; slm4ie/data/synthetic.py is likewise empty.
#
# Reserved for the synthetic-data route: expected to hold LLM API / model
# parameters, prompt templates, and generation/output config once the generator
# is implemented. The schema is not final and is intentionally left blank.
